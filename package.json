{
  "name": "tasks-cli",
  "version": "1.0.0",
  "bin": {
    "tasks": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile --outfile ~/.local/bin/tasks src/cli.ts",
    "install-bin": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
